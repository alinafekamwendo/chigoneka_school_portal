// app/unauthorized/page.tsx
import UnauthorizedContent from "@/components/Unauthorized"; // Adjust the import path

const UnauthorizedPage = () => {
  
  return <UnauthorizedContent />;
};

export default UnauthorizedPage;
